# Built output
build/
app/build/
.gradle/
out/
release/

# Local config
local.properties
*.iml
.idea/
.cxx/

# Captures / profiling
captures/
.externalNativeBuild/

# Logs
*.log
*.hprof

# Keystores — NEVER commit your signing key or password.
*.jks
*.keystore
keystore.properties

# Kotlin / OS noise
.DS_Store
Thumbs.db

# Gradle wrapper JAR — regenerated by `gradle wrapper` on first Android Studio sync.
# (We do ship gradle-wrapper.properties so the right Gradle version is pinned.)
gradle/wrapper/gradle-wrapper.jar
