<?xml version="1.0" encoding="utf-8"?>
<!--
  Default size is 4×4 — that is the smallest size that shows the whole month as a clean
  7-column grid with five or six week rows. The user can resize down to 3×3-ish on
  launchers that allow it (minResizeWidth/Height: 180dp) or up to fill the screen.

  Sizes are given in dp (post-Android-12 sizing). Min/target widths roughly correspond
  to launcher cell counts:
    4×4 cell  ≈ 250 dp × 250 dp  (default / target)
    3×3 cell  ≈ 180 dp × 180 dp  (smallest resize)
-->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:initialLayout="@layout/widget_loading"
    android:minWidth="250dp"
    android:minHeight="250dp"
    android:minResizeWidth="180dp"
    android:minResizeHeight="180dp"
    android:targetCellWidth="4"
    android:targetCellHeight="4"
    android:maxResizeWidth="480dp"
    android:maxResizeHeight="480dp"
    android:resizeMode="horizontal|vertical"
    android:widgetCategory="home_screen"
    android:description="@string/widget_description"
    android:previewLayout="@layout/widget_loading"
    android:updatePeriodMillis="86400000" />
